export function Sidebar() {
  return (
    <aside className="hidden w-56 shrink-0 border-r border-zinc-200 bg-white p-4 md:block">
      <h1 className="text-sm font-semibold">Crawler Agent</h1>
      <p className="mt-2 text-xs text-zinc-500">历史会话将在 V2 提供，当前为单次请求。</p>
    </aside>
  );
}
